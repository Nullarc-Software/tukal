import { App } from "vue";
declare const _default: (Vue: App, options: any) => void;
/**
 * Vuesax global mixin, all vueasx functions and properties injected
 * in the @beforeCreate hook.
 */
export default _default;
